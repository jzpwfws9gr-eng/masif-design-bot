# V84.1.7 - فهد

إصلاح شجرة البطولة بالأعلام الرسمية كملفات PNG داخل دوائر.

## التعديلات
- إضافة ملفات أعلام PNG داخل `assets/flags/`.
- شجرة البطولة تستخدم ملفات الأعلام بدل الإيموجي أو الألوان المرسومة.
- الأعلام داخل دوائر ذهبية، بدون أسماء.
- إجبار إعادة توليد صورة الشجرة بعد التحديث حتى لا يبقى كاش V84.1.6 القديم.
- لا تغيير على `/start`.
- لا تغيير على مصدر نتائج المباريات.
- لا ESPN داخل زر شجرة البطولة أو مسار البطولة.

## مهم في Railway
تأكد من requirements.txt:

```txt
python-telegram-bot[job-queue]
```
